# 唐山皮影持续工作台验证记录

验证日期：2026-09-02

## 本轮结论

九工序已经从“多页、重复点击、精细勾线”重构为单页持续工作台。用户只需要点击、选择和拖拽；描样改为整张转印，不再要求沿皮影边缘逐笔描画。

## 自动检查

- `npm run check`：通过。
- JavaScript 语法：40 个文件通过。
- 单元测试：26/26 通过。
- 文案白名单：38 项通过。
- 资源：12 个运行页面、9 张工序画面、11 个真实部件、4 张皮料通过引用检查。
- 媒体：主包 1,832,505 B，2D 分包 9,232 B，游戏分包 47,685 B，均低于 1.8 MiB。
- `git diff --check`：通过。

## 状态与无死锁检查

- 未选角不能进入工坊。
- 只能进入当前工序，不能跳关。
- 当前工序不到 100% 时，`completeStage()` 拒绝完成。
- 检查点在组件重建后可恢复。
- 重复完成不会二次推进。
- 触摸取消会把部件放回原位，不产生落点事件。
- XR 不可用或 3.2 秒未加载完成时，同页切到 2D，不阻断进度。

## 微信开发者工具走通

运行环境：微信开发者工具，基础库 3.17.2，390×844 模拟器。

走通顺序：

1. 入口与设置浮层。
2. 小旦选角。
3. 识皮：A、D 错误后拖入 B。
4. 制稿：4 个真实结构片归位。
5. 描样：描样纸整体拖入底稿完成转印。
6. 雕刻：点击外轮廓、内部纹样和镂空。
7. 敷彩：五色选择和五个颜色工艺组。
8. 部件处理：5 组代表部件向外分离，显示全部 11 部件结果。
9. 连缀关节：肩、肘、膝连接和一次活动检验。
10. 装杆：三杆安装和一次综合操纵。
11. 灯影检验：拖入灯幕并操纵，转换为剪影。
12. 到达只显示“成品”的终页。

脚本通过组件的 `start/move/end`、`select` 和 `drop` 链路推进，再由页面判定和状态机完成；没有直接写入工序完成状态。开发者工具自身的元素点击协议在当前版本会卡住，因此入口导航使用页面公开动作，不能据此宣称完成了物理鼠标点击或真机触摸验收。

## 截图复核

最终截图位于 `docs/validation/ux-remediation/`，共 15 张：入口、设置、选角、九工序、展开流程、成品和玩法说明。

截图复核结果：

- 设置浮层位于标题下方，不遮挡标题和设置按钮。
- 选角确认按钮固定在底部安全区上方，390×844 首屏可见。
- 折叠流程栏不遮挡皮料、连接件和操纵杆。
- 展开流程栏完整显示九工序，画布未缩放、未位移。
- 制稿显示真实结构片和对应底稿，不再出现大椭圆热点。
- 描样使用与小旦同源的整张转印图，不再产生错位手绘线。
- 点击和拖拽在玩法说明中使用不同手势图形。
- 敷彩的五个可见色点已改为独立点击目标，不再只依赖透明画布坐标；组件测试验证目标事件与防重复逻辑，开发者工具预览编译通过。本次自动化连接在选角导航阶段失败，尚未把真实 WXML `tap` 逐色走通写成完成证据。

## 四种屏幕的自动布局检查

布局计算测试覆盖 360×800、375×812、390×844 和 430×932。检查内容包括顶部安全区、工作台尺寸、归一化坐标、流程栏覆盖方式和 44 px 主要触控区域。

当前只有 390×844 完成逐屏模拟器截图；其余三种尺寸完成的是布局计算测试，不写成视觉或真机验收。

## 待真机验收

- 360×800、375×812、390×844、430×932 的实际刘海和底部安全区。
- 连续拖拽、吸附、回弹、触摸取消和页面滚动冲突手感。
- XR-Frame 装杆、灯影操纵、帧率和加载失败后的 2D 降级。
- 背景音乐循环、反馈音和离开体验页后的全局音频恢复。
